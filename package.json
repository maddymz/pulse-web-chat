{
  "name": "pulse-web-chat",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev --prefix client\" \"sh -c 'cd server && uv run uvicorn src.main:socket_app --reload --port 3001'\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
